# renv loads BiocManager while resolving the pinned Bioconductor release;
# suppress the resulting false-positive pre-activation namespace warning.
Sys.setenv(RENV_CONFIG_NAMESPACES_CHECK = "FALSE")
source("renv/activate.R")
